// Now we trigger the functions
addGrid(row, col);
p1InitSnake(row, col);
p2InitSnake(row, col);
addFood(row, col);
addFood2(row, col);

$("#p1-bullets").html(p1Bullets);
$("#p2-bullets").html(p2Bullets);
